# pytest, pytest-asyncio and pytest-cov are pinned *exactly* by
# pytest-homeassistant-custom-component, so they are deliberately left
# unconstrained here — adding our own floors is what made this file
# unresolvable twice (pytest>=9.1.1, pytest-cov>=7.1.0). Let phcc decide, and
# see the dependabot ignore list.
pytest
pytest-asyncio
pytest-cov
pytest-homeassistant-custom-component>=0.13.357
aiohttp>=3.14.3
israel-rail-api==0.1.5
pre-commit>=4.6.2
ruff>=0.16.3
bandit[toml]>=1.9.4
mypy>=2.3.1
